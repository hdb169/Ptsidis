make a directory called scripts and keep two weighted_av.cxx here. Keep csv files in TextFiles directory. make a 
run the code as root -l -b -q scripts/weighted_av.cxx
This code does take weighted averages by fitting all the similar data sets at same z values and takes the mean and error. 
This is the best method to do weighted averages.
===================================================
